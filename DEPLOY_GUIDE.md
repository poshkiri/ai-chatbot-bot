# 🚀 Полная инструкция: GitHub + Render

## Часть 1: Загрузка кода в GitHub

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** → **"New repository"**
4. Заполните:
   - **Repository name**: `ai-chatbot-bot`
   - **Description**: `AI Telegram Bot with Google Gemini`
   - **Visibility**: `Public` или `Private` (на ваше усмотрение)
   - **НЕ** ставьте галочки на "Add README", "Add .gitignore", "Choose a license"
5. Нажмите **"Create repository"**

### Шаг 2: Инициализируйте Git локально

Откройте терминал в папке `ai-chatbot-bot` и выполните:

```bash
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: AI Telegram Bot"

# Переименование ветки в main (если нужно)
git branch -M main

# Добавление удаленного репозитория
# ЗАМЕНИТЕ your_username на ваш GitHub username
git remote add origin https://github.com/your_username/ai-chatbot-bot.git

# Загрузка кода на GitHub
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Используйте **Personal Access Token** (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Права: `repo` (полный доступ к репозиториям)

### Шаг 3: Проверка

1. Обновите страницу репозитория на GitHub
2. Должны увидеть все файлы проекта

---

## Часть 2: Деплой на Render

### Шаг 1: Создайте аккаунт на Render

1. Зайдите на [render.com](https://render.com)
2. Нажмите **"Get Started for Free"**
3. Выберите **"Sign up with GitHub"**
4. Разрешите доступ к репозиториям

### Шаг 2: Создайте PostgreSQL базу данных

1. В Render Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Заполните:
   - **Name**: `ai-chatbot-db`
   - **Database**: `ai_chatbot_bot`
   - **User**: `ai_chatbot_user`
   - **Region**: Выберите ближайший (например, `Frankfurt`)
   - **PostgreSQL Version**: `16` (или последняя)
   - **Plan**: `Free` (для начала)
3. Нажмите **"Create Database"**
4. **ВАЖНО**: Дождитесь создания (1-2 минуты)
5. Скопируйте **Internal Database URL** (он понадобится позже)
   - Формат: `postgresql://user:password@host:5432/database`

### Шаг 3: Создайте Web Service

1. В Render Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите GitHub репозиторий:
   - Выберите **"Connect account"** (если еще не подключен)
   - Выберите ваш репозиторий `ai-chatbot-bot`
   - Нажмите **"Connect"**

### Шаг 4: Настройте Web Service

**Basic Settings:**
- **Name**: `ai-chatbot-bot`
- **Region**: `Frankfurt` (или тот же, что и БД)
- **Branch**: `main`
- **Root Directory**: `ai-chatbot-bot` (если бот в подпапке, иначе оставьте пустым)

**Build & Deploy:**
- **Environment**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python main.py`

### Шаг 5: Добавьте переменные окружения

В разделе **"Environment Variables"** добавьте:

#### Обязательные переменные:

```
BOT_TOKEN=ваш_токен_от_BotFather
GOOGLE_AI_API_KEY=ваш_google_ai_ключ
AI_PROVIDER=google
DATABASE_URL=вставьте_Internal_Database_URL_из_шага_2
ADMIN_USER_IDS=ваш_telegram_id
```

#### Как получить значения:

1. **BOT_TOKEN**: 
   - Найдите [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/mybots` → выберите вашего бота → `API Token`

2. **GOOGLE_AI_API_KEY**:
   - У вас уже должен быть (из `.env` файла)

3. **DATABASE_URL**:
   - Скопируйте **Internal Database URL** из созданной PostgreSQL базы
   - Формат: `postgresql://user:password@host:5432/database`
   - Код автоматически конвертирует в `postgresql+asyncpg://`

4. **ADMIN_USER_IDS**:
   - Ваш Telegram ID (можно узнать у [@userinfobot](https://t.me/userinfobot))

#### Опциональные переменные (можно оставить по умолчанию):

```
BOT_USERNAME=ваш_бот_username
GOOGLE_AI_MODEL=gemini-pro
LOG_LEVEL=INFO
```

### Шаг 6: Запустите деплой

1. Прокрутите вниз и нажмите **"Create Web Service"**
2. Render начнет сборку (займет 2-5 минут)
3. Следите за логами в реальном времени

### Шаг 7: Проверка работы

1. Откройте раздел **"Logs"** в Render
2. Должны увидеть:
   ```
   ✅ MongoDB подключена (или предупреждение, если не настроена)
   ✅ Redis подключен (или предупреждение, если не настроен)
   ✅ Бот запущен и готов к работе!
   ```

3. Найдите вашего бота в Telegram
4. Отправьте `/start`
5. Бот должен ответить! 🎉

---

## ❌ Решение проблем

### Ошибка: "Build failed"

**Причина**: Неправильный Build Command или отсутствие зависимостей

**Решение**:
- Убедитесь, что `Build Command`: `pip install -r requirements.txt`
- Проверьте, что `requirements.txt` существует в репозитории

### Ошибка: "Bot not starting"

**Причина**: Неправильные переменные окружения

**Решение**:
- Проверьте все переменные в разделе "Environment"
- Убедитесь, что `BOT_TOKEN` и `GOOGLE_AI_API_KEY` правильные
- Проверьте логи в Render

### Ошибка: "Database connection failed"

**Причина**: Неправильный `DATABASE_URL`

**Решение**:
- Используйте **Internal Database URL** (не External!)
- Убедитесь, что база данных создана и запущена
- Формат должен быть: `postgresql://user:password@host:5432/database`

### Ошибка: "Redis connection failed"

**Решение**:
- **Игнорируйте!** Redis опционален
- Бот автоматически переключится на MemoryStorage
- Это нормально для начала

---

## 🔄 Обновление кода

После изменений в коде:

```bash
cd ai-chatbot-bot
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически:
1. Обнаружит изменения
2. Начнет новую сборку
3. Перезапустит бот

---

## 📊 Мониторинг

### Просмотр логов

1. Откройте ваш Web Service в Render
2. Перейдите в раздел **"Logs"**
3. Видите логи в реальном времени

### Метрики

- **CPU Usage**: Использование процессора
- **Memory**: Использование памяти
- **Request Logs**: Логи запросов

---

## 💰 Стоимость

**Free Tier включает:**
- ✅ 750 часов работы в месяц (достаточно для 24/7)
- ✅ PostgreSQL база данных (1 ГБ)
- ⚠️ После 750 часов сервис "засыпает" (можно разбудить)

**Для постоянной работы:**
- Starter план: $7/месяц (без ограничений по времени)

---

## ✅ Чек-лист

- [ ] Код загружен в GitHub
- [ ] PostgreSQL база данных создана
- [ ] Web Service создан
- [ ] Все переменные окружения добавлены
- [ ] Бот запущен и работает
- [ ] Проверена команда `/start` в Telegram

---

## 🎉 Готово!

Ваш бот теперь работает 24/7 в облаке! 🚀

**Что дальше:**
- Обновления кода: просто делайте `git push`
- Render автоматически перезапустит бот
- Логи доступны в реальном времени
- База данных автоматически создаст таблицы при первом запуске

